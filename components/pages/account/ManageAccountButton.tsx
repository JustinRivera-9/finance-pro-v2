"use client";
import { Button } from "@/components/ui/button";

const ManageAccountButton = ({ accessToken }: { accessToken: string }) => {
  return <p className="text-lg text-error">Remove</p>;
};

export default ManageAccountButton;
