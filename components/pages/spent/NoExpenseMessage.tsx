import React from "react";

const NoExpenseMessage = () => {
  return (
    <section>
      <h1>No expenses this month!</h1>
      <button>Add Expense</button>
    </section>
  );
};

export default NoExpenseMessage;
